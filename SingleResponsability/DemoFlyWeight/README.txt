In this project to execute you must run the test cases and check the output of cansole.
After you run both, you will be able to compare the results using a normal approach(TestUser) and using FlyWeight(TestUser2).

To run the tests you must install "JetBrains Resharp extension" so you will be abble to execute tests:
1-Install extension 
2-At side of the test method signature you will se a "icon"
3-Click and select "Run under dotmemory"